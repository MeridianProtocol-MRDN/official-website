# Public, non-secret values only. Never put secrets in VITE_* variables —
# anything prefixed VITE_ is bundled into the client-side build and is
# publicly visible.

VITE_PUBLIC_TOKEN_ADDRESS=
VITE_PUBLIC_CHAIN_ID=56
